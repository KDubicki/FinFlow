"""The vocabulary every other layer shares.

This package imports nothing else from ``finflow``. Anything placed here is a
type that more than one layer needs to agree on, which is the only reason for a
definition to live this far inward.
"""
